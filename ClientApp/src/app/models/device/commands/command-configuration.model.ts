import { CommandMethodModel } from "./command-method.model";

export class CommandConfigurationModel {
  public maxActions: number;
  public maxCCC: number;
  public maxOptions: number;
  public maxDefault: number;

}
